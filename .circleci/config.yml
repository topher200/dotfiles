version: 2.1
jobs:
  build:
      docker:
        - image: ubuntu:latest
      steps:
        - run:
            name: make install
            command: make install
workflows:
  version: 2
  build:
    jobs:
      - build
