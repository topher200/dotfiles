version: 2.1
      
jobs:
  build:
      docker:
        - image: ubuntu:latest
      steps:
        - run: apt update && apt install make
        - checkout
        - run: make install
workflows:
  version: 2
  build:
    jobs:
      - build
