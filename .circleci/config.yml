version: 2.1

jobs:
  test:
    docker:
      - image: ubuntu:21.10
    steps:
      - checkout
      - run: sudo apt install -y make
      - run: make test-in-docker
workflows:
  version: 2
  build_and_test:
    jobs:
      - test
