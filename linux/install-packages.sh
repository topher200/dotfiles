# install packages
sudo apt install -y \
     autojump \
     thefuck

# install node and npm, from https://github.com/nodesource/distributions#installation-instructions
curl -sL https://deb.nodesource.com/setup_13.x | sudo -E bash -
sudo apt-get install -y nodejs
