#!/bin/bash
# Wrapper for CircleCI MCP server - update with: npm install -g @circleci/mcp-server-circleci@latest
NODE_BIN=$(find "$HOME/micromamba/envs/memfault/.volta/tools/image/node/" -mindepth 2 -maxdepth 2 -name bin -type d 2>/dev/null | tail -1)
exec "$NODE_BIN/mcp-server-circleci" "$@"
